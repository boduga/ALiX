# SDD ledger — plan: docs/superpowers/plans/2026-08-12-cap-8-capability-service-surface.md

Plan human-reviewed; 12 rulings locked (10 from the initial grilling + 2 clarifications post-plan-writer: #11 narrow `getLifecycleState` accessor on CapabilityResolver; #12 required `eventLog` ctor dep on CapabilityPlatform). BASE for the whole branch: `f2768108` (plan amendment commit on top of `0d42d79d` CAP-7 merge).

**Standing constraints:**
- Forbidden files (never touch): `src/capability/initial-capabilities.ts`, `src/tools/tool-registry.ts`, `src/policy/capability-registry.ts`, production `src/capability/canonical/*`, pre-CAP-7 TUI façade `src/tui/capabilities/capability-service.ts` (CAP-11 migration debt — Task 7 touched this per ruling #12; `ALLOWED_BUT_TRACKED` allowlist in cap-8-supersession.test.ts)
- Test conventions: vitest for `tests/capability/*` (`.vitest.ts`); node:test for supersession (`cap-8-supersession.test.ts`); run `pnpm exec vitest run tests/capability/`
- ESM strict mode (`"type": "module"`) — frozen writes throw TypeError
- `beforeEach` not suite-level `before` (id@version collision pattern from CAP-6)
- Never use bare `git stash`/`git stash pop` (shared stash across worktrees)
- `tsc --noEmit` and `pnpm exec vitest run` are gates
- 9-task plan (per ruling-coverage matrix)
- No push without human approval
- Hard architectural boundary per locked ruling #2 — composition root constructs registry/resolver only

**Opus whole-branch review (added 2026-08-12):** READY-TO-MERGE, confidence 4/5. All 12 locked rulings independently verified. 7 fixable-before-merge Minors identified; user cherry-picked 2 (Task 6 M1 history limit tail semantics + Task 3 M2 dead overrideUsed local). Remaining 7 parked. **CAP-8 invariant:** new capability-service consumers use `CapabilityService`; legacy CLI `apply` remains temporarily on `CapabilityLifecycleApplier` solely as a tracked CAP-11 removal surface.

Task 1: complete (commits f2768108..4bf8e3eb, implementer DONE_WITH_CONCERNS — 3 concerns all addressed inline, transparent report). 11/11 new tests pass; 312/312 across `tests/capability/` (35 files); tsc 0 errors. Concerns: (1) brief verbatim `expect(Keys.prototype.hasOwnProperty)` runtime/type bug — `keyof` is compile-time-only; replaced with non-functional string-array assertion; (2) brief verbatim import paths used `service-results.ts` (one dir too shallow) and `capability/kind.ts` (doesn't exist) — corrected inline to `types/service-results.ts` and `canonical/kind.ts`; (3) `Object.freeze(this)` on error class is acceptable per ruling #4 but flagged for CAP-9/10 subclassing. Plan amended at same lines (brief + plan). Reviewer (sonnet) reported Spec ✅ + Approved-with-Minor (4 Minors: M1 four-dep test is a null/object tautology; M2 no-envelope test is `a.every(k => a.includes(k))` tautology — runtime weak but compile-time gates solid; M3 report miscounts `@ts-expect-error` as 4 (actual 3); M4 `CapabilityApplyInput.step` inner fields not readonly — per brief verbatim, not blocking). All Minors deferred to final review. BASE for Task 2: 4bf8e3eb.

Task 2: complete (commits 4bf8e3eb..82d12d80, implementer DONE — 5 brief verbatim bugs fixed inline). Implementer added the public `CapabilityResolver.getLifecycleState(id)` accessor (locked ruling #11) — required for the broadened service to read lifecycle without a back-channel cast. New read-test file replaces CAP-7's delegation test (CAP-7 stub retired per ruling #stub). 7/7 new read tests pass; 312/312 across `tests/capability/` (35 files); tsc 0 errors. Brief verbatim bugs fixed inline: (1) `import { type Capability } from "./registry.js"` — registry doesn't export `Capability`; replaced with `CapabilityDefinition` from `./canonical/definition.js`; (2) wrong depth `../../../adaptation/...` (3 `..`); corrected to `../../adaptation/...` (2 `..`) per `src/capability/`; (3) `search` text filter `id || title` broken against default-title test data; changed to id-only; (4) no-op `q.tags.every(...)` filter dropped (test invariant already holds); (5) `Object.freeze(...)` applied to returned arrays to satisfy runtime mutation tests. Plan amended at same lines (brief + plan). Reviewer (sonnet) reported Spec ✅ + Approved-with-Minor (5 Minors: M1 `resolverAvailable` + `availabilityOf` duplication — could share one helper; M2 `resolverAvailable` naming could be `isAvailable`; M3 unused `_ctx` parameter per brief verbatim — kept for symmetry; M4 test title advertises `tags` filter but impl no longer filters by tags — title drift; M5 `lifecycleOf` JSDoc slightly ironic about service-reads-only — comment drift). Confidence 5/5. All Minors deferred to final review. BASE for Task 3: 82d12d80.

Task 3: complete (commits 82d12d80..5918147e, implementer DONE_WITH_CONCERNS — 4 brief verbatim bugs fixed inline). New `health()` method delegates to `CapabilityResolver.resolve()` and aggregates into narrow `CapabilityHealthResult` (ruling #9 — never `ProviderCandidate[]`). 7/7 new tests pass; 319/319 across `tests/capability/` (37 files); tsc 0 errors. Brief verbatim bugs fixed inline: (1) test "missing binding" expected `reason="missing_binding"` but setup (1 binding of unregistered type) yields `bindingsCount=1, candidates=0` → correct reason is `provider_unavailable`; (2) `'external-cli'` binding requires `config.executable` per `validateProviderBinding`; brief's `config: {}` threw; (3) brief prose listed reason order as `provider_unavailable` before `missing_binding` but Step-3 code does the opposite — followed code; (4) implementer used user-provided commit message verbatim (matches dispatch prompt). Plan amended at same lines (brief + plan). Reviewer (sonnet, confidence 4/5) reported Spec ✅ + Approved-with-Minor (4 additional Minors: M1 no-op try/catch around resolver.resolve — both branches throw; M2 dead `overrideUsed` variable + `void overrideUsed;` suppressor; M3 step-undefined fallback `false → lifecycle_ineligible` inconsistent with `availabilityOf`'s `missing_binding` for same condition; M4 stale comment about brief prose order — references older draft). All 4 Minors deferred to final review. BASE for Task 4: 5918147e.

Task 4: complete (commits 5918147e..46b18517, implementer DONE_WITH_CONCERNS — 4 brief verbatim bugs fixed inline, all real CAP-6 seam misunderstandings). `apply()` delegates verbatim to CAP-6 `CapabilityMutationExecutor.executeStep()` — no second mutation execution path (ruling #1 invariant intact). 4/4 new tests pass; 323/323 across `tests/capability/` (38 files); tsc 0 errors. Brief verbatim bugs fixed inline: (1) test `parameters` were missing `operation` discriminator that CAP-5 `validateMutationMutation` requires — added; (2) `result.output.artifactId` should be `result.output.result.artifactId` (CAP-6 success path returns `{ operation, mutation, result }`); (3) `expect(...).toMatch(/^ar-/)` should be `/^[a-f0-9]{64}$/` since CAP-6 `artifactId` is SHA-256 hex; (4) `registry.has` doesn't exist on `CapabilityRegistry` — replaced with `getLifecycleState(...).toBeUndefined()`. Plus implementer concern: `affected` projection is heuristic (computed from `parameters` since CAP-6 never sets `output.affected`) — flagged as future refinement for consolidate which can over-report. Plan amended at same lines (brief + plan). Reviewer (sonnet, confidence 5/5) reported Spec ✅ + Approved-with-Minor (3 Minors: M1 `affectedFromResult` reads `params.definition.id` as a fourth fallback — brief incompleteness fix, not YAGNI; M2 `affected` over-reports on consolidate (sources + target) — acceptable for CAP-8, CAP-9/10 tighten; M3 `readArtifactId` cast helper workaround for `noUncheckedIndexedAccess`+`strict: true` — typed `CapabilityMutationResult` import would eliminate it but would leak CAP-6's type into service's public surface, correct trade-off). Reviewer independently verified CAP-6 seam end-to-end by reading all 531 lines of `capability-mutation-executor.ts` — confirmed entry point `executeStep`, success shape `{ operation, mutation, result }`, SHA-256 hex artifactId, all 12+ failure paths return `output = {}`. All 3 Minors deferred to final review. BASE for Task 5: 46b18517.

Task 5: complete (commits 46b18517..1b6a0408, implementer DONE — 1 brief verbatim bug fixed inline, 1 extra test added for `recommend()` read-only invariant). `propose()` and `measure()` forward-wired stubs throw `CapabilityServiceNotImplementedError(code: "not_implemented_yet")` (ruling #4 stable contract); `recommend()` confirmed read-only via structural pin against A7 proposal builder imports. 6/6 new tests pass; 329/329 across `tests/capability/` (39 files); tsc 0 errors. Brief verbatim bug fixed inline: dead `const before = 'capability.echo'` (unused under strict) removed. Implementer also flagged a downstream subtlety for final review: `Object.freeze(this)` on `CapabilityServiceNotImplementedError` blocks CAP-9/CAP-10 from attaching context fields post-construction (acceptable per ruling #4 — the "same contract" applies to the error, not parameter types; CAP-9/CAP-10 can replace the constructor if they need richer errors). Plan amended at same lines (brief + plan). Reviewer (sonnet, confidence 4/5) reported Spec ✅ + Approved-with-Minor (2 Minors: M1 inline comment drift — comment says "capability-evolution-types writers" but regex forbids "capability-evolution-intelligence"; M2 tautological assertion `expect(res.total).toBe(res.suggestions.length)` — `recommend()` constructs `total: suggestions.length` by definition). Reviewer also confirmed: implementer's downstream `Object.freeze` concern is NOT a blocker — ruling #4 "same contract" covers class identity + `code` literal, not parameter types; CAP-9/CAP-10 have three non-breaking paths if they need context fields. All 2 Minors deferred to final review. BASE for Task 6: 1b6a0408.

Task 6: complete (commits 1b6a0408..3362065f, implementer DONE — 4 brief verbatim bugs fixed inline, all real EventLog API misunderstandings). `history(capabilityId, options?)` is a narrow EventLog projection filtered by `capabilityId` (ruling #5 invariant intact — no catalog reconstruction). 5/5 new tests pass; 334/334 across `tests/capability/` (40 files); tsc 0 errors. Brief verbatim bugs fixed inline: (1) `eventLog.append` 2-arg signature wrong — real signature is single-object `{ type, actor, sessionId, payload }`; (2) `(e as { at?: unknown }).at` wrong field — `AlixEvent` has `timestamp` not `at`; (3) structural sentinel regex against whole file would fail unconditionally (pre-existing `catalog.get`/`catalog.list` in `list()`/`inspect()`) — scoped to `history()` method body + added `catalog.listPublications` + `registry.` pins; (4) brief asked to re-export `AlixEvent` from capability-service — declined (type belongs to events module); added `CapabilityHistoryEvent`/`CapabilityHistoryResult` to existing `export type` block. EventLog API verified at `src/events/event-log.ts:233` (`readAll(): Promise<AlixEvent[]>`) with `AlixEvent` shape at `src/events/types.ts:12`. Implementer concern: `opts.limit` returns LAST N events ("tail" semantics) — future change to first-N would break contract. Plan amended at same lines (brief + plan). Reviewer (sonnet, confidence 4/5) reported Spec ✅ + Approved-with-Minor (5 Minors: M1 `limit` test doesn't lock tail-vs-head semantics — would silently pass on `slice(0, limit)` change; M2 `at: e.timestamp` mapping is uncovered by tests; M3 sentinel regex `/async history\([\s\S]*?\n  \}/` is brittle against 2-space-indent blocks/comments inside body — robust enough to catch targeted regression; M4 report deviation #4 misframes "brief asked → I declined" when actually brief already declined; M5 trivial style — `let filtered = matched; if (...)` could be one-line conditional). All 5 Minors deferred to final review. BASE for Task 7: 3362065f.

Task 7: complete (commits 3362065f..603ac840, implementer DONE_WITH_CONCERNS — 4 concerns flagged). `CapabilityPlatform` extended with `readonly service: CapabilityService` field wired from the four deps (ruling #6 + ruling #12 invariants intact). 3/3 new tests pass; 337/337 across `tests/capability/` (41 files); tsc 0 errors. Concerns: (1) brief verbatim `eventLog?: EventLog` (optional) contradicted ruling #12 — implementer correctly made it REQUIRED with throw guard; (2) TUI bootstrap auto-constructs an EventLog if caller omits it — non-violation since production TUI callers supply their own; flagged for review; (3) barrel left untouched per brief ("no change needed") — `CapabilityServiceNotImplementedError` and result-shape types reachable only via explicit relative paths; (4) pre-existing CI failure in `alix-submit-cli.vitest.ts` confirmed unrelated via baseline stash-test. Plan amended at same lines (brief + plan). Reviewer (sonnet, confidence 5/5) reported Spec ✅ + **Approved (no Minors)** — first clean approval of the SDD run. Reviewer independently verified: production-source grep confirms `CapabilityPlatform` is sole `new CapabilityRegistry()` + `new CapabilityResolver()` site; throw-guard `if (!opts.eventLog) throw` at platform.ts:47-49 with message naming ruling #12; all 5 existing CAP-1/3/5/6/7 platform test sites updated (`platform.vitest.ts` ×3, `platform-projection.vitest.ts` ×3, `tool-adapter.vitest.ts` ×1 via `platformWithTool` helper); production bootstrap wired at `src/cli.ts:2224-2227` and `src/tui/capabilities/capability-service.ts:67-69`; pre-existing CI failure attribution verified via diff. BASE for Task 8: 603ac840.

Task 8: complete (commits 802f33ea..50275c6c, implementer DONE on resume — sentinel 3/3, capability suite 340/340, CLI migration 2/2, full sweep 4517/4524, tsc 0 errors). 6 files changed (+239/-23). Three-axis structural sentinel + CLI capabilities migration complete. Implementer self-documented 5+ brief-bug fixes inline: (1) axis 2 regex tightened to anchor on `capability/` module path (`capability/(?:registry|provider-resolver)\.js`) — brief's compressed `[^"']*(?:registry|provider-resolver)\.js` over-matched `policy/capability-registry.js` (pre-CAP-2 different class); (2) CAP-11 debt allowlist added — 5 pre-existing files importing `CapabilityRegistry` outside `src/capability/**` listed in `CAP11_DEBT_FILES: ReadonlySet<string>` with comment forbidding new entries: `capability-lifecycle-rehydration.ts`, `capability-lifecycle-applier.ts`, `capability-lifecycle-step-executor.ts`, `capability-mutation-executor.ts`, `session-capabilities.ts`; (3) `CapabilitiesCLIDeps.registry` re-typed as `unknown` — A7.0 applier is CAP-11 debt, cast at boundary uses `ConstructorParameters<typeof CapabilityLifecycleApplier>[0]["registry"]`; (4) docstring in `src/cli/commands/capabilities.ts` rephrased — original "new CapabilityRegistry(" phrase tripped axis 3 grep; (5) CLI migration scope bounded to the 8 commands listed; `alix capabilities apply <id>` keeps legacy registry param (CAP-11 debt). Concerns: CAP-11 debt allowlist pressure (5 files is right for CAP-8 scope but future PRs could expand one unnoticed); axis 3 grep-only scope (sibling CLI files would NOT be caught); `registry?: unknown` structurally looser but tsc enforces shape compatibility at applier cast site. Plan amended at the locked clarifications section (item #5). Reviewer (sonnet, confidence 4/5) reported Spec ✅ + Approved-with-Minor (2 Minors: M1 stale docstring at `capability-lifecycle-cli.ts:29` saying `Parameters<>` but code uses `ConstructorParameters<>` — fix docstring to match code; M2 broad `try/catch (unknown)` in `renderInspect` — acceptable for byte-identical CLI UX but narrowed catch would harden contract). Reviewer independently verified axis 1 + axis 2 + axis 3 sentinel test results, the 8 CLI command migrations, the integration test that builds `CapabilityService` via composition-root shape, and the 5-file CAP-11 debt allowlist (comment explicit). All 2 Minors deferred to final review. BASE for Task 9: 50275c6c.

Task 9: complete (commits 50275c6c..04a86ebf, implementer DONE_WITH_CONCERNS — 2 brief verbatim bugs fixed inline, all green). CAP-8 supersession forbidden-file test + final sweep complete. ALL GREEN: capability suite 340/340, full vitest 4517/4524 (7 skipped + 1 skipped-file pre-existing), tsc 0 errors, build 0 errors, supersession `Tests 2 passed (2)`. Brief verbatim bugs fixed inline: (1) TUI façade allowlist added — Task 7 modified `src/tui/capabilities/capability-service.ts` to satisfy locked ruling #12's EventLog-supplied-by-composition-root invariant; without allowlist supersession would fail (verified 1 match in diff). Added `ALLOWED_BUT_TRACKED: ReadonlySet<string>` with explicit ruling #12 rationale, mirroring Task 8's `CAP11_DEBT_FILES` pattern; (2) silent fallthrough on shallow-clone CI without `main` ref — followed CAP-7's actual pattern (not the brief's mention of CAP-6's `assert.ok(resolved)` hard guard). Side-effect: test vacuously passes on shallow-clone CI instead of hard-failing; flagged for future CAP-N. Implementer also noted: report/brief files remain untracked (per Task 8 precedent, only `progress.md` committed). Plan amended at same lines (brief + plan). Reviewer (sonnet, confidence 4/5) reported Spec ✅ + Approved-with-Minor (1 Minor: M1 hardcoded directory prefix `p.startsWith("src/capability/canonical/")` inlined at call site vs. brief's `FORBIDDEN_DIR_PREFIXES: readonly string[]` array — equally readable today (one directory) but future maintainers would edit predicate body rather than data structure; refactor back to array if a second forbidden directory is ever added; not blocking). Both brief-bug fixes accepted (TUI façade allowlist structurally identical to Task 8 `CAP11_DEBT_FILES` precedent; silent fallthrough follows CAP-7's actual pattern, brief is internally contradictory on this point — implementer correctly chose CAP-7). All 1 Minor deferred to final review. **All 9 SDD tasks complete. Next: final opus whole-branch review.**

Task 9: complete (commits df099c1b..<pending>, implementer DONE_WITH_CONCERNS). Supersession forbidden-file guard + final sweep. New `tests/capability/cap-8-supersession.test.ts` (67 lines incl. docstring) added; mirrors CAP-7 pattern (silent `execSync` fallthrough, `git diff --name-only main...HEAD`). Brief verbatim bugs fixed inline: (1) `src/tui/capabilities/capability-service.ts` is in the diff (modified by Task 7 for ruling-12 `eventLog` wiring) — added as the SINGLE entry in `ALLOWED_BUT_TRACKED: ReadonlySet<string>` with explicit ruling-12 rationale, mirroring the precedent Task 8 set for the 5-file `CAP11_DEBT_FILES` allowlist. File remains in `FORBIDDEN` array; allowlist exclusion is documented and narrow. Verified the test would fail without the allowlist via direct `git diff --name-only --diff-filter=AM main...HEAD | grep src/tui` (returns 1 match); with the allowlist, 2/2 tests pass. (2) brief mentions CAP-6 `assert.ok(resolved, "main ref required")` guard — CAP-7's actual pattern (immediate predecessor; brief verbatim says "Mirrors CAP-6/CAP-7 supersession test pattern") uses silent fallthrough, not a hard guard. Followed CAP-7's pattern. Result: 2/2 supersession tests pass; full vitest sweep 4517/4524 (7 skipped + 1 skipped-file pre-existing); `pnpm exec tsc --noEmit` exit 0; `pnpm run build` exit 0; capability suite 340/340. CAP-8 GREEN at the test supersession gate. Concerns: (a) TUI façade narrow exception is precedent, not blanket approval — CAP-11 must amend brief + plan for any further changes; (b) silent `execSync` fallthrough vacuously passes on shallow-clone CI without `main` ref — consider adding `assert.ok(resolved, ...)` guard in CAP-N; (c) Task 9 is the LAST task per ruling-coverage matrix (9 tasks total) — frontier = CAP-9/#501.
