{
  "post_task": [
    {
      "command": "node ~/.alix/skills/langfuse-traces/scripts/query.mjs --trace-id <TRACE_ID> --limit 20",
      "reason": "Tiered trace inspect: summary always, detail on failure. Replace <TRACE_ID> per run (runner gap: no run-id interpolation yet — see README)."
    }
  ]
}
