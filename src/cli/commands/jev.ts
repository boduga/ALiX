/**
 * `alix jev` — decision-subsystem operator surface.
 *
 * #717 convention: implementation lives in `./jev/*.ts`; this barrel preserves
 * the public import surface.
 *
 * @module
 */

export { handleJevCommand } from "./jev/main.js";
