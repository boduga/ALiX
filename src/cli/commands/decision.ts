/**
 * P6.0a — decision CLI command.
 *
 * #717 — the implementation lives in `./decision/*.ts`; this barrel preserves
 * the public import surface.
 *
 * @module
 */

export { handleDecisionCommand } from "./decision/main.js";
