# P9.4b — Policy Coverage Mutation

> **Status:** SDS approved
> **Risk level:** Low (additive to proven P9.4a infrastructure)
> **Spec home:** `docs/superpowers/specs/2026-06-24-p9-4b-policy-coverage-design.md`
> **Plan home:** `docs/superpowers/plans/2026-06-24-p9-4b-policy-coverage.md`
> **Governs:** branch from `main` at the P9.4a merge base.

## Core framing

P9.4b adds `policy_coverage` as a third `SUPPORTED_KIND` in the existing `GovernanceChangeApplier`. All infrastructure (mutation lock, atomic writes, snapshot, drift detection, pre-write hash check, governance mutation evidence) is reused unchanged.

## Target file

`.alix/governance/policy-coverage.json`

```json
{
  "currentCoverage": 0.62,
  "targetCoverage": 0.80,
  "updatedByProposalId": "prop-...",
  "updatedAt": "2026-06-24T..."
}
```

## Mutation semantics

`policy_coverage` adjusts the **desired coverage target**, not measured current coverage.

| Step | Logic |
|------|-------|
| Drift guard | `file.currentCoverage === payload.currentCoverage` |
| Mutation | `file.targetCoverage = payload.targetCoverage` |
| Stamping | `updatedByProposalId = proposal.id`, `updatedAt = new Date().toISOString()` (generated by applier, not from payload) |
| Missing file | Fail closed (no auto-create) |
| Invalid schema | Fail closed (require numeric `currentCoverage`, `targetCoverage`) |

## Implementation surface

All changes in `src/adaptation/appliers/governance-change-applier.ts`:

| Change | Detail |
|--------|--------|
| `SUPPORTED_KINDS` | Add `"policy_coverage"` |
| Named constant | `POLICY_COVERAGE_FILE = "policy-coverage.json"` |
| `resolveTargetFile()` | New `case "policy_coverage"` |
| `validateSchema()` | Assert `typeof data.currentCoverage === "number"` and `typeof data.targetCoverage === "number"` |
| `validateCurrentValues()` | Assert `file.currentCoverage === payload.currentCoverage` |
| `applyMutation()` | Set `targetCoverage`, stamp `updatedByProposalId` + `updatedAt` |

## Unchanged

- Mutation lock (process-local promise-chain mutex)
- Snapshot + pre-write hash check (same sequence)
- Atomic write (write-tmp → fsync → rename)
- `governance_mutation_applied` evidence (payloadKind distinguishes kind)
- `selectApplier()` (no new routing — same `target.kind === "governance"`)
- No new evidence types, no new writer methods, no sentinel changes

## Testing

Tests in `tests/adaptation/appliers/governance-change-applier.vitest.ts`:

1. `applies policy_coverage successfully` — create policy-coverage.json, apply, verify file updated
2. `rejects policy_coverage with drift` — currentCoverage mismatch
3. `rejects missing policy-coverage file` — file does not exist
4. `rejects invalid policy_coverage schema` — missing numeric fields
5. `records governance_mutation_applied for policy_coverage` — assert payloadKind === "policy_coverage"

## Explicitly out of scope

| Feature | Notes |
|---------|-------|
| Per-policy-area coverage | Future — P9.4b keeps single global config |
| Auto-create on missing file | Fail closed; bootstrap slice separately |
| Schema migration | Not needed — new file, no existing data |
| New evidence infrastructure | P9.4a's `governance_mutation_applied` handles all kinds |
