## Summary
Create a comprehensive repository structure map in `DIRECTORY.md` and reference it in the `README.md` to document the layout of the codebase.

## Changes

- **Action:** create
- **File:** `DIRECTORY.md`
- **Description:** Document the project's directory structure (similar to an annotated `tree` output) to provide a clear overview of where source files, assets, configuration, and tests reside.

- **Action:** modify
- **File:** `README.md`
- **Description:** Add a "Project Structure" section linking to `DIRECTORY.md` to ensure developers can easily find the codebase map.

## Verification
- Run a directory listing command (e.g., `find . -maxdepth 3` or `tree -I "node_modules|.git"`) to ensure the documented tree in `DIRECTORY.md` accurately matches the physical file structure.
- Verify markdown rendering and link integrity in a local markdown viewer or GitHub preview.

## Risk Assessment
- **Risk level:** low
- **Blast radius:** Documentation only; no functional code is touched, so there is zero risk of breaking application behavior.
- **New dependencies:** None.